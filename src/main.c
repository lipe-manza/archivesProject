#include <stdio.h>
#include ""

int main()
{
    int n = 0;

    scanf("%d", &n);

    switch (n)
    {
        case 1:
        char scv[50];
        char bin[50];
        FILE *p_bin;

        p_bin = scv_to_bin(scv, bin);
        break;
    }
}
